# Headers (inc)

This directory contains public header files for the ADAS project (function prototypes and shared definitions).

Add header files (.h) here. Do not add compiled artifacts (object files, executables) — those belong in the build output and should be ignored via `.gitignore`.

Example files:
- seatbelt.h
- sensor.h
- vehicle_logic.h
- warning.h
